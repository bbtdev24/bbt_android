<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Background transparan -->
    <corners android:radius="6dp" /> <!-- Rounded corners 6dp -->
    <stroke
        android:width="1dp"
        android:color="@color/green" />
</shape>
